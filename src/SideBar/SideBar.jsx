import { SideBar } from "./style";
import { RoundDiv } from "./style";

const SideDrawer = () => {
  return (
    <SideBar>
      <RoundDiv></RoundDiv>
    </SideBar>
  );
};

export default SideDrawer;
